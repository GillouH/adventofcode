# adventofcode

Version de Python : 3.12.1

Pour lancer un script main, il faut se déplacer dans le dossier puis lancer la commande "python main.py". Si le script est lancé depuis un autre endroit "python <path_to_folder>/main.py", celà ne fonctionnera pas.
